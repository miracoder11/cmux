# Phase 3 Plan

## Tasks

- Add a Git activity view inside `VerticalTabsSidebar`.
- Read selected workspace metadata from `TabManager`.
- Show branch and dirty state.
- Show file status counts from `FileExplorerStore.gitStatusByPath`.
- Show current directories and pull requests when available.
- Add a shortcut to open the Git activity.

## Verification

- Confirm the view composes from existing workspace/store data.
- Validate build through fork CI.
